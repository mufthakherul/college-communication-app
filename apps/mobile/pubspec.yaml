name: campus_mesh
description: RPI Communication App - Rangpur Polytechnic Institute (Developed by Mufthakherul)

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase Core Services (All FREE)
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.6.0
  firebase_messaging: ^14.7.10
  firebase_analytics: ^10.8.0
  # Note: cloud_functions removed - using client-side logic instead
  # See CLOUD_FUNCTIONS_FREE_ALTERNATIVE.md for details
  
  # Other Dependencies
  url_launcher: ^6.2.1
  webview_flutter: ^4.4.2
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true