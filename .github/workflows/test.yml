name: Mobile Extended CI (Legacy to be pruned)

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled in favor of ci-mobile.yml"
