name: Automated Database Backup (Disabled)

on:
  # Disabled by default; enable manually if you really use Supabase backups
  workflow_dispatch:

jobs:
  backup:
    if: false # Disabled; remove this to re-enable if Supabase is actually used
    name: Backup Supabase Database
    runs-on: ubuntu-latest
    steps:
      - run: echo "Supabase backup workflow disabled"
